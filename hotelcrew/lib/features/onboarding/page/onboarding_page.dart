import 'package:flutter/material.dart';
import '../../../main.dart';

class OnboardingPage extends State<MyHomePage> {
  OnboardingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(
      backgroundColor: Colors.white,
    ));
  }
}